# 이 저장소에서 일하는 방식

대화가 길어지면 앞부분이 요약으로 압축되면서 아래 내용이 사라진다.
그래서 메모리가 아니라 여기에 둔다. 매 요청마다 다시 읽힌다.

## 반드시 지킬 것

**1. 짐작하지 말고 물어본다.**
지시가 애매하면 그 자리에서 되묻는다. 제 나름대로 해석해서 만들어 놓고
나중에 "이게 아니다" 소리를 듣는 것이 제일 나쁘다.
사용자는 되묻는 것을 싫어하지 않는다 — 말없이 제 판단대로 만드는 것을 싫어한다.

**2. 손대기 전에 승인받는다.**
판단하지 말라는 뜻이 아니다. 판단은 하되 무엇을 왜 바꿀지 먼저 말하고,
"해라" 소리를 듣고 나서 손댄다. 욕을 들었다고 "아무것도 판단 안 하겠다"로
도망가는 것도 똑같이 틀렸다.

**3. 요청한 것만 건드린다.**
SG 얘기 중에 IAM을 고치는 식의 곁다리 작업 금지.
바꾸고 싶은 게 생기면 말부터 한다.

**4. 질문에는 먼저 답한다.**
답을 피하고 다음 작업 얘기로 넘어가지 않는다.
한 번 말한 것은 그걸로 끝나야 한다. 두 번 말하게 만들지 않는다.

**5. 디자인은 시안을 먼저 보여준다.**
내 머릿속에서 만들지 말고 실제 제품을 찾아보고, 찾은 구조를 그대로 옮긴다.
말로 설명하지 말고 눌러볼 수 있는 것으로 만든다. 그리고 화면에 띄워 준다.

**6. 안 된다고 하면 내 코드부터 의심한다.**
캐시나 화면 착각은 내 코드를 다 뒤진 다음에 꺼낸다.
사용자는 확인하고 말한다. 나도 확인하고 말한다.

## 배포 — "Published"는 끝이 아니다

`npx gh-pages -d dist` 가 Published를 찍어도 GitHub Pages 빌드가 실패하면
서비스에는 안 나간다. 실제로 그래서 사용자가 몇 번이나 "안 바뀌었다"고
말하게 만들었다. 그러니 마지막에 한 번 더 본다.

```bash
npm run build
npx gh-pages -d dist
gh api -X POST repos/seonggyun951-ship-it/security-project/pages/builds

# 서비스되는 파일이 방금 만든 것과 같은지 확인
want=$(grep -o 'assets/index-[^.]*\.js' dist/index.html)
curl -s "https://seonggyun951-ship-it.github.io/security-project/" | grep -o 'assets/index-[^.]*\.js'
```

두 값이 같아야 반영된 것이다. 다르면 기다렸다 다시 확인한다.
Pages 상태는 `gh api repos/seonggyun951-ship-it/security-project/pages --jq .status`.

## 도구

- DB는 `supabase db query --linked -f <파일>` 로 직접 실행한다.
  (`db push` 는 오래된 마이그레이션에서 막히므로 해당 파일만 직접 적용한다)
- 소스 수정은 Edit 도구로만. sed·perl 같은 일괄 치환 금지.
- 삭제(파일·리소스·데이터)는 반드시 승인받고 한다.

## 화면 만들 때 쓰는 틀

AWS 현황 · 보안 점검 결과 · 점검 이력이 같은 틀을 쓴다. 새 화면도 여기 맞춘다.

- **상단** — `--band` 그라디언트 띠에 먼저 알아야 할 숫자 몇 개
- **왼쪽** — 개수가 붙은 패싯. 여러 개를 동시에 고를 수 있다
- **가운데** — 표. 머리글 12px 대문자, 본문 14.5px, 줄 높이 46px, 종류 구분줄
- **오른쪽** — 상세 패널. 아코디언으로 아래를 밀지 않는다
- **바닥** — 몇 건 중 몇 건, 쪽 넘김

위험/주의 판정은 **보안 점검 한 곳에서만** 한다. 다른 화면에서 따로 판정하면
Prowler 결과와 어긋났을 때 어느 쪽을 믿을지 알 수 없게 된다.

목록은 어느 단계에서도 통째로 쏟지 않는다. 계정 → VPC → 대상 으로 눌러 들어가고
한 번에 25개씩 그린다.
